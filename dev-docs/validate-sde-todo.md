# Validate SDE TODO

Validate the SDE using typeddicts and pydantic, collecting data on missing or additional data files, number of validation errors, and number of records in each file. Offer the results of the validation as a cli summary, a json file, and text file report. Validate the entire SDE, or an individual file. Support marking some files as not able to validate.